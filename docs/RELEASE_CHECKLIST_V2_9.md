# V2.9.0 体验版发布记录

## 2.9.1 游戏体验更新（2026-08-10）

- [x] 后端完整测试：`59 passed`。
- [x] Taro 微信生产构建成功。
- [x] 敏感信息扫描通过（286 个候选文件）。
- [x] GitHub `main` 推送提交 `cc41df6`。
- [x] Render OpenAPI 版本切换为 `2.9.1`，五子棋、飞行棋、斗地主已暴露兼容的人机创建参数。
- [x] 微信开发者工具 CLI 上传 `2.9.1`，代码包约 748.9KB。
- [x] 微信公众平台显示版本 `2.9.1`、提交时间 `2026-08-10 17:55:25`，并标记为体验版。
- [ ] 两台真机完成联机、切后台、断线恢复和弱网验收。
- [x] 使用免费的 PostgreSQL 图片 provider，移除 R2 凭据阻断。

2.9.1 可用于体验测试；上述两项仍是正式稳定发布前的外部验收项。

发布日期：2026-08-10

## 代码与数据

- [x] 版本号更新为 `2.9.0`。
- [x] 发布前 Neon 生产备份完成（API 逻辑备份已校验）。
- [x] 空库及 V2.0 基线 Alembic 升级通过。
- [x] 后端完整自动测试通过。
- [x] 小程序生产构建通过。
- [x] 微信开发者工具结构冒烟通过。
- [x] 敏感信息与发布配置扫描通过。

## 线上服务

- [x] GitHub main 已推送 2.9.0 提交。
- [x] Render 自动部署成功（含已有索引兼容迁移修复）。
- [x] `/api/health` 返回正常，OpenAPI 版本为 `2.9.0`。
- [ ] `/api/ready` 的数据库与存储均为 ready。
- [x] 新设备会话、菜单加载、管理登录与订单隔离线上验证通过。
- [ ] PostgreSQL 图片上传及 HTTPS 图片读取通过。

生产图片已切换到免费的 PostgreSQL 持久化 provider，不再依赖缺失的 S3/R2 凭据；部署后需确认 `/api/ready` 为 `ready` 并完成一次真实图片上传/读取。

## 微信体验版

- [x] 开发者工具上传版本 `2.9.0`（代码包约 738 KB）。
- [x] 微信公众平台开发版本中显示 `2.9.0`。
- [x] 设为体验版。
- [ ] 两个体验设备完成点餐和在线游戏基本验证。

该文件随发布过程实时更新；未完成项不得在交付说明中写成已通过。
